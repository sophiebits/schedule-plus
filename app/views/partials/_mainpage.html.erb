
			<li id="main-page" class="page">
				<div id="bg-mask"></div>
				<div class="container">
					<div class="sidebar">
						<ul id="main-nav">
							<li id="main-nav-schedules"><%= link_to "schedule", schedules_path, :class => "selected nav-link" %></li>
							<li id="main-nav-friends"<% if !current_user %> class="locked"<% end %>>
                <a class="nav-link"<% if current_user %> href="friends"<% end %>>friends</a>
								<% if !current_user %>
                <span class="tooltip">
									<script type="text/javascript">
                  
                  </script>
								  <%= link_to "Connect", "/auth/facebook", :id => 'fb-connect' %>	
							  </span><% end %>
              </li>
						  <% if current_user %>
              <li id="main-nav-logout"><%= link_to 'logout', 'sessions', :method => 'delete', :class => 'nav-link' %></li>
              <% end %>
					  </ul>
					</div>
					<div id="main-content">
          <%= yield :content %>
					</div>
				</div>
			</li>
